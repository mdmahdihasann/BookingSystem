import React from 'react'

export const metadata = {
    title: "Dashboard - Launches Booking",
};

const Dashboard = () => {
  return (
    <div>page is the dashboard</div>
  )
}

export default Dashboard